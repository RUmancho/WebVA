# Чеклист миграции на версию 2.0

## ✅ Перед обновлением

### 1. Создайте резервную копию

```bash
# Резервная копия базы данных
cp users.db users_backup_$(date +%Y%m%d_%H%M%S).db

# Резервная копия конфигурации
cp app_config.json app_config_backup.json
```

### 2. Сохраните текущую версию

```bash
# Если используете git
git branch pre-v2-backup
git commit -am "Backup before v2 migration"
```

---

## 📦 Установка

### 1. Обновите зависимости

```bash
pip install -r requirements.txt
```

**Новые зависимости:**
- numpy - для оптимизации
- aiofiles - для асинхронных операций
- apscheduler - для планировщика

### 2. Проверьте установку

```bash
python -c "import numpy, aiofiles, apscheduler; print('OK')"
```

Должно вывести: `OK`

---

## 🔧 Конфигурация

### 1. Обновите app_config.json

Добавьте новые секции:

```json
{
  "database_advanced": {
    "max_db_requests_per_minute": 100,
    "connection_pool_size": 10,
    "connection_pool_recycle": 3600,
    "query_timeout_seconds": 30,
    "bulk_operation_batch_size": 500
  },
  "scheduler": {
    "enabled": true,
    "class_attachment_check_interval_minutes": 3,
    "auto_cleanup_interval_hours": 24,
    "max_concurrent_tasks": 5
  }
}
```

### 2. Настройте под ваши нужды

**Для небольших систем (< 100 пользователей):**
```json
{
  "database_advanced": {
    "connection_pool_size": 5,
    "bulk_operation_batch_size": 100
  },
  "scheduler": {
    "max_concurrent_tasks": 2
  }
}
```

**Для крупных систем (> 1000 пользователей):**
```json
{
  "database_advanced": {
    "connection_pool_size": 20,
    "bulk_operation_batch_size": 1000,
    "max_db_requests_per_minute": 200
  },
  "scheduler": {
    "max_concurrent_tasks": 10
  }
}
```

---

## 🗄️ База данных

### 1. Автоматическая миграция

При первом запуске SQLAlchemy автоматически создаст новую таблицу `class_attachment_tasks`.

**Ничего делать не нужно!**

### 2. Проверка миграции

После первого запуска проверьте, что таблица создана:

```python
from database import db
session = db.get_session()
from models import ClassAttachmentTask
tasks = session.query(ClassAttachmentTask).all()
print(f"Таблица создана. Задач: {len(tasks)}")
```

---

## 🚀 Запуск

### 1. Первый запуск

```bash
streamlit run main.py
```

### 2. Проверьте логи

В консоли должно появиться:

```
База данных успешно инициализирована с SQLAlchemy
Планировщик успешно инициализирован (интервал: 3 мин)
Задача автоприкрепления добавлена (каждые 3 мин)
Планировщик запущен
```

### 3. Проверьте планировщик

Откройте приложение и войдите как учитель. Если все работает - планировщик активен.

---

## ✅ Проверка функциональности

### 1. Базовая проверка

- [ ] Приложение запускается без ошибок
- [ ] Вход в систему работает
- [ ] Регистрация работает
- [ ] Все старые функции доступны

### 2. Проверка новой функциональности

- [ ] Вкладка "Учителя" открывается
- [ ] Видна секция "Автоматическое прикрепление класса"
- [ ] Форма создания задачи отображается
- [ ] Можно создать задачу прикрепления

### 3. Тест автоматического прикрепления

#### Шаг 1: Создайте учителя
```
Email: teacher@test.com
Пароль: teacher123
Роль: Учитель
Город: Тестовый
Школа: Тест Школа №1
```

#### Шаг 2: Войдите как учитель и создайте задачу
```
Город: Тестовый
Школа: Тест Школа №1
Класс: 10А
Количество: 5
```

#### Шаг 3: Создайте ученика
```
Email: student1@test.com
Пароль: student123
Роль: Ученик
Город: Тестовый
Школа: Тест Школа №1
Класс: 10А
```

#### Шаг 4: Подождите 3 минуты

#### Шаг 5: Проверьте результат
- [ ] Войдите как учитель
- [ ] Проверьте прогресс задачи (должен быть 1/5)
- [ ] Проверьте уведомления
- [ ] Проверьте дерево учеников

#### Шаг 6: Войдите как ученик
- [ ] Проверьте, что учитель появился в разделе "Мои учителя"
- [ ] Проверьте уведомление о прикреплении

---

## 🔍 Проверка производительности

### 1. Пул соединений

```python
from database import db
print(f"Pool size: {db.engine.pool.size()}")
```

### 2. Планировщик

```python
from scheduler import get_scheduler_status
status = get_scheduler_status()
print(status)
```

Должно вывести:
```python
{
    'running': True,
    'jobs_count': 1,
    'jobs': [{'id': 'class_attachment_check', ...}],
    'message': 'Планировщик работает нормально'
}
```

---

## 🐛 Устранение неполадок

### Проблема 1: Ошибка импорта модулей

```
ImportError: No module named 'numpy'
```

**Решение:**
```bash
pip install numpy aiofiles apscheduler
```

### Проблема 2: Планировщик не запускается

```
scheduler_enabled: False
```

**Решение:** Проверьте `app_config.json`:
```json
{
  "scheduler": {
    "enabled": true
  }
}
```

### Проблема 3: Ошибка БД

```
OperationalError: no such table: class_attachment_tasks
```

**Решение:** Удалите БД и перезапустите:
```bash
# ВНИМАНИЕ: Это удалит все данные!
rm users.db
streamlit run main.py
```

Или создайте таблицу вручную:
```python
from database import db
db.init_database()
```

### Проблема 4: Конфликт портов

```
Port 8501 is already in use
```

**Решение:**
```bash
streamlit run main.py --server.port 8502
```

---

## 📊 Мониторинг

### 1. Логи планировщика

Следите за логами в консоли:
```
[scheduler.py] Начало обработки задач прикрепления
[scheduler.py] Найдено активных задач: 2
[database.py] Задача 1: прикреплено 3 учеников
```

### 2. Проверка задач

Периодически проверяйте активные задачи:
```python
from database import db
tasks = db.get_active_attachment_tasks()
for task in tasks:
    print(f"Задача {task['id']}: {task['current_student_count']}/{task['target_student_count']}")
```

---

## 🔄 Откат (если что-то пошло не так)

### 1. Остановите приложение

```bash
Ctrl+C
```

### 2. Восстановите резервную копию

```bash
# Восстановление БД
cp users_backup_*.db users.db

# Восстановление конфига
cp app_config_backup.json app_config.json
```

### 3. Откатите код (если используете git)

```bash
git checkout pre-v2-backup
```

### 4. Переустановите старые зависимости

```bash
pip install streamlit openai sqlalchemy
```

---

## 📈 Оптимизация после миграции

### 1. Настройте интервал проверки

Если у вас редко регистрируются новые ученики, увеличьте интервал:
```json
{
  "scheduler": {
    "class_attachment_check_interval_minutes": 10
  }
}
```

### 2. Настройте размер батчей

Для больших классов (> 40 учеников):
```json
{
  "database_advanced": {
    "bulk_operation_batch_size": 1000
  }
}
```

### 3. Мониторинг нагрузки

Следите за использованием ресурсов:
```bash
# CPU и память
top -p $(pgrep -f "streamlit")

# Размер БД
ls -lh users.db
```

---

## ✅ Финальный чеклист

### Предварительная подготовка
- [ ] Создана резервная копия БД
- [ ] Создана резервная копия конфига
- [ ] Сохранена текущая версия кода

### Установка
- [ ] Установлены новые зависимости
- [ ] Проверена установка модулей
- [ ] Обновлен app_config.json

### Запуск
- [ ] Приложение запущено без ошибок
- [ ] Планировщик инициализирован
- [ ] Создана таблица class_attachment_tasks

### Тестирование
- [ ] Базовая функциональность работает
- [ ] Старые функции не сломались
- [ ] Новая система автоприкрепления работает
- [ ] Уведомления отправляются
- [ ] Прогресс отображается корректно

### Производительность
- [ ] Планировщик работает (каждые 3 мин)
- [ ] Нет ошибок в логах
- [ ] Пул соединений активен
- [ ] Нагрузка в норме

### Документация
- [ ] Прочитан UPGRADE_SUMMARY.md
- [ ] Прочитан QUICK_START_RU.md
- [ ] Команда ознакомлена с изменениями

---

## 🎯 Следующие шаги

### После успешной миграции:

1. **Обучите пользователей:**
   - Учителей - как создавать задачи
   - Учеников - что ожидать

2. **Мониторинг первую неделю:**
   - Следите за логами
   - Проверяйте корректность прикреплений
   - Собирайте обратную связь

3. **Оптимизация:**
   - Подстройте интервалы под вашу нагрузку
   - Настройте размеры батчей
   - Отрегулируйте пул соединений

4. **Развитие:**
   - Изучите возможности дальнейшей оптимизации
   - Рассмотрите добавление аналитики
   - Планируйте будущие улучшения

---

## 📞 Поддержка

### В случае критических проблем:

1. Сделайте откат (см. раздел "Откат")
2. Соберите логи ошибок
3. Проверьте секцию "Устранение неполадок"
4. Обратитесь к документации

### Полезные документы:
- `UPGRADE_SUMMARY.md` - полная документация
- `QUICK_START_RU.md` - руководство пользователя
- `app_config.json` - настройки
- Логи в консоли - диагностика

---

**Дата создания:** 2025-11-03  
**Версия:** 2.0  
**Статус:** ✅ Готов к использованию

**Успешной миграции! 🚀**

