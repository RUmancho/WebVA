# Руководство по тестированию системы с несколькими пользователями

## Подготовка к тестированию

### 1. Создание тестовых пользователей

Запустите скрипт для создания тестовых пользователей:

```bash
python scripts/create_test_users.py
```

Это создаст:
- 3 тестовых ученика
- 3 тестовых учителя

### 2. Запуск приложения с доступом по сети

#### Вариант 1: Использование готового скрипта (Windows)
```bash
UI\run_network.bat
```

#### Вариант 2: Ручной запуск
```bash
cd UI
python.exe -m streamlit run main.py --server.address 0.0.0.0 --server.port 8501
```

#### Вариант 3: Создание конфигурационного файла

Создайте файл `.streamlit/config.toml` в корне проекта:

```toml
[server]
address = "0.0.0.0"
port = 8501
enableCORS = false
enableXsrfProtection = false
```

Затем запускайте обычным способом:
```bash
UI\run.bat
```

### 3. Определение IP-адреса

После запуска приложение покажет Network URL, например:
```
Network URL: http://192.168.0.103:8501
```

Используйте этот адрес для доступа с других устройств.

## Тестовые учетные данные

### Ученики:
- **student1@test.com** / **student123** (Иван Петров, 10А)
- **student2@test.com** / **student123** (Мария Сидорова, 9Б)
- **student3@test.com** / **student123** (Алексей Козлов, 11В)

### Учителя:
- **teacher1@test.com** / **teacher123** (Анна Иванова, Математика)
- **teacher2@test.com** / **teacher123** (Дмитрий Смирнов, Физика/Химия)
- **teacher3@test.com** / **teacher123** (Елена Волкова, Русский язык)

## Сценарии тестирования

### Сценарий 1: Тестирование теории
1. Войдите как ученик (student1@test.com)
2. Перейдите в раздел "📚 Теория"
3. Выберите предмет → раздел → тему
4. Дождитесь генерации объяснения
5. Откройте чат по теме (кнопка "💬 Задать вопрос по теме")
6. Задайте несколько вопросов по теме
7. Проверьте, что ответы релевантны и без раздумий

### Сценарий 2: Тестирование взаимодействия ученик-учитель
1. Войдите как ученик (student1@test.com)
2. Перейдите в "👨‍🏫 Учителя"
3. Найдите учителя (teacher1@test.com)
4. Отправьте заявку учителю
5. Войдите как учитель (teacher1@test.com)
6. Проверьте заявку в разделе "📋 Заявки"
7. Примите заявку
8. Проверьте связь в "👨‍🏫 Учителя" / "👨‍🎓 Мои учащиеся"

### Сценарий 3: Тестирование с нескольких устройств
1. Запустите приложение с доступом по сети
2. Откройте приложение на разных устройствах/браузерах
3. Войдите под разными пользователями
4. Проверьте, что сессии не пересекаются
5. Проверьте функционал чата, теории, тестирования

## Важные замечания

### Безопасность
⚠️ **ВНИМАНИЕ**: Запуск с `--server.address 0.0.0.0` делает приложение доступным для всех в вашей сети. Используйте это только для тестирования в локальной сети!

### Производительность
- Каждый пользователь создает отдельную сессию Streamlit
- LLM запросы обрабатываются последовательно
- При большом количестве пользователей может потребоваться оптимизация

### Ограничения Streamlit
- Streamlit не предназначен для production с множеством одновременных пользователей
- Для production рекомендуется использовать более мощные решения (FastAPI + React, Django, Flask и т.д.)

## Устранение проблем

### Проблема: Не могу подключиться с другого устройства
**Решение:**
1. Проверьте, что устройства в одной сети
2. Проверьте настройки файрвола Windows
3. Убедитесь, что приложение запущено с `--server.address 0.0.0.0`

### Проблема: Ошибка "Address already in use"
**Решение:**
1. Измените порт: `--server.port 8502`
2. Или остановите другие процессы на порту 8501

### Проблема: Сессии пересекаются
**Решение:**
- Это нормальное поведение Streamlit - каждая вкладка браузера имеет свою сессию
- Используйте разные браузеры или режим инкогнито для разных пользователей

