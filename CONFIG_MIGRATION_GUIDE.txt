╔═══════════════════════════════════════════════════════════════════════════════╗
║                  РУКОВОДСТВО ПО НОВОЙ СИСТЕМЕ КОНФИГУРАЦИИ                    ║
╚═══════════════════════════════════════════════════════════════════════════════╝

📋 ОБЗОР
────────────────────────────────────────────────────────────────────────────────
Все настройки проекта теперь централизованы в одном месте:
  📁 app_config.json - единый файл конфигурации
  🐍 config_manager.py - модуль для работы с настройками

════════════════════════════════════════════════════════════════════════════════

📂 СТРУКТУРА app_config.json
────────────────────────────────────────────────────────────────────────────────

{
  "app": {                    // Настройки приложения
    "title": "...",
    "page_icon": "🎓",
    "layout": "wide"
  },
  
  "ai_models": {              // ⭐ Настройки AI моделей
    "default_model": "gpt-4o-mini",
    "temperature": 0.3,       // Легко изменить здесь!
    "max_tokens": 2000,
    "top_p": 1.0,
    "frequency_penalty": 0.0,
    "presence_penalty": 0.0
  },
  
  "logging": {                // 📝 Управление логами
    "enabled": {
      "llm.py": false,        // Отключить логи для llm.py
      "auth.py": true         // Включить логи для auth.py
    },
    "ai_logs": true
  },
  
  "features": {               // 🎛️ Переключатели функций
    "enable_ai_assistant": true,
    "enable_video_calls": false
  }
}

════════════════════════════════════════════════════════════════════════════════

🚀 БЫСТРЫЙ СТАРТ
────────────────────────────────────────────────────────────────────────────────

1. ПРОСМОТР ТЕКУЩИХ НАСТРОЕК
   └─ Откройте: app_config.json

2. ИЗМЕНЕНИЕ НАСТРОЕК
   └─ Отредактируйте нужные значения в JSON файле
   └─ Сохраните файл
   └─ Перезапустите приложение (если необходимо)

3. ИСПОЛЬЗОВАНИЕ В КОДЕ

   ┌─ СТАРЫЙ СПОСОБ (config.py) ─────────────────────────────────┐
   │ import config                                                 │
   │ temperature = 0.3  # Жестко заданное значение               │
   └───────────────────────────────────────────────────────────────┘
   
   ┌─ НОВЫЙ СПОСОБ (config_manager.py) ───────────────────────────┐
   │ from config_manager import get_ai_temperature                │
   │ temperature = get_ai_temperature()  # Из app_config.json     │
   └───────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════════════════

💡 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ
────────────────────────────────────────────────────────────────────────────────

┌─ ПРИМЕР 1: Настройки AI ──────────────────────────────────────────────────┐
│                                                                             │
│  from config_manager import (get_ai_model, get_ai_temperature,             │
│                              get_ai_max_tokens)                             │
│                                                                             │
│  # В вашем коде LLM                                                        │
│  response = client.chat.completions.create(                                │
│      model=get_ai_model(),              # Из конфига: "gpt-4o-mini"       │
│      temperature=get_ai_temperature(),  # Из конфига: 0.3                 │
│      max_tokens=get_ai_max_tokens(),    # Из конфига: 2000                │
│      messages=[...]                                                        │
│  )                                                                          │
│                                                                             │
│  💡 Изменение в app_config.json мгновенно применится при следующем запуске │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ ПРИМЕР 2: Управление логами ─────────────────────────────────────────────┐
│                                                                             │
│  from config_manager import log_info, log_error, is_logging_enabled        │
│                                                                             │
│  MODULE_NAME = "llm.py"                                                    │
│                                                                             │
│  # Вместо обычного print                                                  │
│  log_info(MODULE_NAME, "Обработка запроса...")                            │
│  log_error(MODULE_NAME, f"Ошибка: {error}")                               │
│                                                                             │
│  # В app_config.json:                                                     │
│  # "logging": {                                                           │
│  #   "enabled": {                                                         │
│  #     "llm.py": false  ← Эти логи НЕ будут выводиться                   │
│  #   }                                                                    │
│  # }                                                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

┌─ ПРИМЕР 3: Проверка функций ──────────────────────────────────────────────┐
│                                                                             │
│  from config_manager import is_feature_enabled                             │
│                                                                             │
│  if is_feature_enabled('enable_ai_assistant'):                            │
│      # Код AI ассистента                                                  │
│      init_ai_assistant()                                                   │
│  else:                                                                     │
│      print("AI ассистент отключен в конфигурации")                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

════════════════════════════════════════════════════════════════════════════════

📚 ДОСТУПНЫЕ ФУНКЦИИ
────────────────────────────────────────────────────────────────────────────────

🤖 НАСТРОЙКИ AI
  • get_ai_model() - название модели
  • get_ai_temperature() - temperature (0.0 - 2.0)
  • get_ai_max_tokens() - максимум токенов
  • get_ai_top_p() - top_p параметр
  • get_ai_frequency_penalty() - frequency penalty
  • get_ai_presence_penalty() - presence penalty
  • get_ai_timeout() - таймаут запросов
  • get_ai_max_retries() - количество повторов
  • get_available_models() - список доступных моделей

📝 ЛОГИРОВАНИЕ
  • log_info(module, message) - информация
  • log_error(module, message) - ошибки
  • log_warning(module, message) - предупреждения
  • log_debug(module, message) - отладка
  • is_logging_enabled(module) - проверка статуса

🎓 НАСТРОЙКИ ПРИЛОЖЕНИЯ
  • get_app_title() - название приложения
  • get_page_icon() - иконка страницы
  • get_page_config() - конфиг для Streamlit
  • get_database_name() - имя БД
  • get_database_url() - URL БД

✅ ВАЛИДАЦИЯ
  • get_min_name_length() - мин. длина имени
  • get_min_password_length() - мин. длина пароля

💬 ЧАТ
  • get_chat_bot_name() - имя бота
  • get_chat_system_message() - системное сообщение
  • get_chat_max_history() - макс. история

🎛️ ФУНКЦИИ
  • is_feature_enabled(feature_name) - проверка функции

🔧 УНИВЕРСАЛЬНЫЕ
  • get_config(section, key, default) - получить любую настройку
  • update_config(section, key, value) - обновить настройку
  • save_config(config_dict) - сохранить конфиг

════════════════════════════════════════════════════════════════════════════════

🔄 МИГРАЦИЯ СУЩЕСТВУЮЩЕГО КОДА
────────────────────────────────────────────────────────────────────────────────

1. НАЙДИТЕ ЖЕСТКО ЗАДАННЫЕ ПАРАМЕТРЫ
   
   ❌ БЫЛО:
   temperature = 0.3
   model = "gpt-4o-mini"
   
   ✅ СТАЛО:
   from config_manager import get_ai_temperature, get_ai_model
   temperature = get_ai_temperature()
   model = get_ai_model()

2. ЗАМЕНИТЕ PRINT НА УСЛОВНОЕ ЛОГИРОВАНИЕ
   
   ❌ БЫЛО:
   print(f"[INFO] Обработка данных: {data}")
   
   ✅ СТАЛО:
   from config_manager import log_info
   MODULE_NAME = "your_file.py"
   log_info(MODULE_NAME, f"Обработка данных: {data}")

3. ИСПОЛЬЗУЙТЕ ФЛАГИ ФУНКЦИЙ
   
   ❌ БЫЛО:
   # Код AI всегда выполняется
   result = call_ai_function()
   
   ✅ СТАЛО:
   from config_manager import is_feature_enabled
   if is_feature_enabled('enable_ai_assistant'):
       result = call_ai_function()

════════════════════════════════════════════════════════════════════════════════

⚙️ ЧАСТО ИЗМЕНЯЕМЫЕ НАСТРОЙКИ
────────────────────────────────────────────────────────────────────────────────

🔥 ТОП-5 настроек для изменения в app_config.json:

1. "ai_models" → "temperature": 0.3
   └─ Контролирует креативность AI (0.0 = строгий, 2.0 = креативный)

2. "ai_models" → "default_model": "gpt-4o-mini"
   └─ Модель OpenAI по умолчанию

3. "logging" → "enabled" → "llm.py": false
   └─ Включить/выключить логи для конкретного модуля

4. "logging" → "ai_logs": true
   └─ Глобальное управление AI логами

5. "features" → "enable_ai_assistant": true
   └─ Включить/выключить AI ассистента

════════════════════════════════════════════════════════════════════════════════

📖 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ
────────────────────────────────────────────────────────────────────────────────

📄 Примеры кода:       config_usage_example.py
📄 Менеджер конфига:   config_manager.py
📄 Файл конфига:       app_config.json
📄 Обратная совм.:     config.py (deprecated)

Для запуска примеров:
  python config_usage_example.py

════════════════════════════════════════════════════════════════════════════════

✨ ПРЕИМУЩЕСТВА НОВОЙ СИСТЕМЫ
────────────────────────────────────────────────────────────────────────────────

✓ Все настройки в одном месте (app_config.json)
✓ Легко изменять без правки кода
✓ Управление логами по модулям
✓ Переключатели функций
✓ Единые настройки AI для всего проекта
✓ Обратная совместимость с config.py

════════════════════════════════════════════════════════════════════════════════

💡 СОВЕТ: Сохраните резервную копию app_config.json перед внесением изменений!

════════════════════════════════════════════════════════════════════════════════

